import styled from 'styled-components';

export const Container = styled.div`
  color: #fff;

  .dropdown_header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-direction: row;

    li {
      list-style: none;
    }
  }
`;
